<!DOCTYPE html>
<html lang="en">
<head>
    
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= food.text %></title>
</head>
<body>
    <%- include('./partials/_navbar.ejs') %>
  <h1><%= food.text %></h1>
  <% if(food.isInPantry){ %>
    <p>You have this food in your pantry....</p>
  <% } else { %>
    <p>You do not have this food in your pantry</p>
  <% } %>
  <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">Edit <%= food._id %></a>
  <form action="/users/<%=user._id%>/foods/<%= food._id %>?_method=DELETE" method="POST">
    <button type="submit">
      Delete <%= food._id %>
    </button>
  </form>
  <a href="/users/<%= user._id %>/foods/<%= food._id %>">Edit <%= food._id %></a>
  <a href="/users/<%= user._id %>/foods">Back to Foods</a>
</body>
</html>