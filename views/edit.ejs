<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/assets/css/styles.css"/>
  <title>Edit Pantry with id <%= food._id %></title>
</head>

<body>
    <%- include('partials/_navbar.ejs') %>
    <h1>Edit <%= food._id %> !</h1>
  <form action="/users/<%=user._id%>/foods/<%= food._id %>?_method=PUT" method="POST">
    <label for="food">Food Text:</label>
    <input type="text" name="food" id="food" value="<%= food.food %>" />
    <label for="isInPantry">Is this food in the pantry?</label>
    <input 
       type="checkbox" 
       name="isInPantry" 
       id="isInPantry" 
       <% if(food.isInPantry){ %>
         checked
       <% } %> />
    <button type="submit">Edit Food</button>
  </form>
  <a href="/users/<%= user._id %> /foods/<%= food._id %>">Back to Pantry Show Page for <%=food._id %></a>
</body>
</html>